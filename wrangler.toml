name = "netgit-proxy"
main = "worker/src/proxy-worker.js"
compatibility_date = "2024-01-01"

# KV 命名空间绑定
[[kv_namespaces]]
binding = "KV"
# ID 在部署时会自动绑定到你选择的 KV 命名空间
# 你在 Cloudflare Pages 设置中已经配置了环境变量 KV

# 定时触发器：每30分钟执行一次健康检查
[triggers]
crons = ["*/30 * * * *"]
